/**
 * TUI View Module — Terminal UI view adapter (Issue #414)
 */

export {
  type TuiConversationRef,
  type TuiMessageRef,
  type TuiOutput,
  TuiViewAdapter,
  tuiTarget,
} from './tui-view-adapter.js';
